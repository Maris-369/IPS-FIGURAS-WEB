// gatsby-config.js (Mínimo - ¡Añade tus plugins!)
module.exports = {
    siteMetadata: {
      title: `IPS Figuras SST Cúcuta SAS`, // Puedes ajustar
      siteUrl: `https://www.trabajosenalturasipsfigurascucuta.com`, // Tu URL
    },
    plugins: [
      // --- ¡¡AQUÍ DEBES AÑADIR LOS PLUGINS QUE TU SITIO USA!! ---
  
      
    
  
      // Revisa tu package.json y añade aquí otros plugins
      // como 'gatsby-plugin-image', 'gatsby-source-filesystem', etc.
      // Ejemplo: `gatsby-plugin-image`,
  
    ],
  }